public class DivisionEntreCero extends Exception {
    // Creación de una nueva clase de excepción mediante la subclase Exception.
    public DivisionEntreCero(String mensaje) {
        super(mensaje);
    }
}
